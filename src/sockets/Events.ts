export enum ConnectivityEvents {
  CONNECT = "connect",
  DISCONNECT = "disconnect",
}

export enum OrderEvents {
  CREATE_NEW = "create-new-order",
  UPDATE = "update-order",
}

export enum ProductEvent {
  PRODUCT_QUANTITY_CHANGE = "product_quantity_change",
}

export enum NotificationEvents {
  CREATE_NEW = "create-new-notification",
}
